// Dashboard.jsx

import React from 'react';

const Dashboard = () => {
  return (
    <div className="dashboard-container">
      <h2>Welcome to the Dashboard</h2>
      <p>This is a protected route that only logged-in users can see.</p>
    </div>
  );
};

export default Dashboard;
